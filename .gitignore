# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Build artifacts
*.bin
# ...except theme fonts, which are the theme, not a build product
!src/theme_assets/**/font_*.bin
!src/theme_assets/**/fonts/*.bin
*.elf
*.map
build/
dist/

# Secrets (WiFi creds live in NVS on the device, never in the repo)
secrets.h

# ESP-IDF component manager (unused; project builds via PlatformIO)
CMakeLists.txt
dependencies.lock
managed_components/
.dummy/

# Generated staging trees, not source
sdcard_stage/
sim/
data/

# Working asset dumps (source of truth is elsewhere; too large/churny for git)
tools/data/
tools/hands/

# Watch-script runtime state
orb_watch.log
orb_watch.pid
