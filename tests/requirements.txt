PyYAML
# Pinned: tools/fallout_art.py (and the other art scripts) draw with Pillow, and the committed images are compared
# pixel for pixel against a redraw. A different Pillow anti-aliases edge pixels differently (CI failed on the
# latest one), so the version that drew the art is the version the tests use. Bump it together with the art.
Pillow==10.4.0
numpy
